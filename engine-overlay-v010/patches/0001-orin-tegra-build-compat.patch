diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4b310d2..af63930 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -61,7 +61,8 @@ endif()
 set_ifndef(CUDA_CTK_VERSION 12.8)
 set_ifndef(CUDA_DIR /usr/local/cuda-${CUDA_CTK_VERSION})
 
-if(NOT DEFINED AARCH64_BUILD)
+if(NOT DEFINED AARCH64_BUILD
+   AND NOT CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
   set(CMAKE_CUDA_ARCHITECTURES 80;86;89)
   if(CUDA_CTK_VERSION VERSION_GREATER_EQUAL 12.8)
     list(APPEND CMAKE_CUDA_ARCHITECTURES 100a 120)
@@ -69,7 +70,13 @@ if(NOT DEFINED AARCH64_BUILD)
 endif()
 
 # Orin/JetPack cuda.h may lack cudaLibrary_t; GDN runner uses compat typedef
-# when this is set.
+# when this is set. Auto-detect Tegra so Jetson native builds don't have to
+# pass -DEMBEDDED_TARGET=jetson-orin manually (otherwise the cuteDSL prebuilt
+# headers fail to compile with 90 "cudaLibrary_t undefined" errors).
+if(NOT DEFINED EMBEDDED_TARGET AND EXISTS "/etc/nv_tegra_release")
+  set(EMBEDDED_TARGET "jetson-orin")
+  message(STATUS "Detected Tegra (L4T); defaulting EMBEDDED_TARGET=jetson-orin")
+endif()
 if(DEFINED EMBEDDED_TARGET AND EMBEDDED_TARGET STREQUAL "jetson-orin")
   add_compile_definitions(TRT_EDGELLM_CUDA_LIBRARY_T_COMPAT)
 endif()
@@ -89,6 +96,16 @@ find_library(
   HINTS ${CUDA_DIR} ${CUDA_TARGET_DIR}
   PATH_SUFFIXES lib lib64 lib/stubs)
 
+find_library(
+  CUBLAS_LIB cublas
+  HINTS ${CUDA_DIR} ${CUDA_TARGET_DIR}
+  PATH_SUFFIXES lib lib64)
+
+find_library(
+  CUBLASLT_LIB cublasLt
+  HINTS ${CUDA_DIR} ${CUDA_TARGET_DIR}
+  PATH_SUFFIXES lib lib64)
+
 find_path(
   CURAND_KERNEL_INCLUDE_DIR
   NAMES curand_kernel.h
diff --git a/cmake/CuteDsl.cmake b/cmake/CuteDsl.cmake
index 936757f..8d20838 100644
--- a/cmake/CuteDsl.cmake
+++ b/cmake/CuteDsl.cmake
@@ -875,20 +875,29 @@ function(cute_dsl_setup)
   foreach(_tgt ${ARG_LINK_TARGETS})
     get_target_property(_tgt_type ${_tgt} TYPE)
     if(_tgt_type STREQUAL "STATIC_LIBRARY")
-      target_link_libraries(${_tgt} PUBLIC "${_static_lib}")
+      target_link_libraries(${_tgt} PUBLIC "${_static_lib}"
+                                           trt_edgellm_cutedsl_cudart_shim)
     else()
       target_link_libraries(${_tgt} PRIVATE "${_static_lib}"
                                             trt_edgellm_cutedsl_cudart_shim)
     endif()
     if(CUDA_DRIVER_LIB AND NOT CUDA_DRIVER_LIB MATCHES "-NOTFOUND$")
-      target_link_libraries(${_tgt} PRIVATE "${CUDA_DRIVER_LIB}")
+      if(_tgt_type STREQUAL "STATIC_LIBRARY")
+        target_link_libraries(${_tgt} PUBLIC "${CUDA_DRIVER_LIB}")
+      else()
+        target_link_libraries(${_tgt} PRIVATE "${CUDA_DRIVER_LIB}")
+      endif()
     endif()
     # CUDA 12.0–12.6: wrap _cudaLaunchKernelEx (cudaKernel_t → CUfunction, e.g.
     # JetPack 6). CUDA 11.x artifacts use CUmodule ABI instead.
     if(NOT _cute_dsl_cuda_ver STREQUAL ""
        AND _cute_dsl_cuda_ver VERSION_GREATER_EQUAL 12.0
        AND _cute_dsl_cuda_ver VERSION_LESS 12.8)
-      target_link_options(${_tgt} PRIVATE "-Wl,--wrap=_cudaLaunchKernelEx")
+      if(_tgt_type STREQUAL "STATIC_LIBRARY")
+        target_link_options(${_tgt} INTERFACE "-Wl,--wrap=_cudaLaunchKernelEx")
+      else()
+        target_link_options(${_tgt} PRIVATE "-Wl,--wrap=_cudaLaunchKernelEx")
+      endif()
     endif()
   endforeach()
 
diff --git a/cpp/CMakeLists.txt b/cpp/CMakeLists.txt
index 1853009..1f1fbc1 100644
--- a/cpp/CMakeLists.txt
+++ b/cpp/CMakeLists.txt
@@ -73,11 +73,23 @@ endif()
 include(${CMAKE_SOURCE_DIR}/cmake/XQACubins.cmake)
 edgellm_xqa_cubins_setup()
 
+function(filter_macos_metadata out_var)
+  set(_items ${${out_var}})
+  list(FILTER _items EXCLUDE REGEX "/\\._[^/]*$")
+  list(FILTER _items EXCLUDE REGEX "/\\.DS_Store$")
+  set(${out_var}
+      "${_items}"
+      PARENT_SCOPE)
+endfunction()
+
 # Generate Core C++ static library for LLM-SDK
 file(GLOB_RECURSE COMMON_CPP_SRCS "common/*.cpp")
 file(GLOB_RECURSE KERNELS_CPP_SRCS "kernels/*.cpp")
 edgellm_xqa_add_generated_sources(KERNELS_CPP_SRCS)
 file(GLOB_RECURSE KERNELS_CU_SRCS "kernels/*.cu")
+filter_macos_metadata(COMMON_CPP_SRCS)
+filter_macos_metadata(KERNELS_CPP_SRCS)
+filter_macos_metadata(KERNELS_CU_SRCS)
 # Exclude MoE Marlin ops/sm80 - compiled only via include from moeMarlin.cu
 list(FILTER KERNELS_CU_SRCS EXCLUDE REGEX
      "marlin_moe_wna16/(ops|sm80_kernel_float16_u4_float16)\\.cu$")
@@ -87,9 +99,15 @@ file(GLOB_RECURSE MULTIMODAL_CPP_SRCS "multimodal/*.cpp")
 file(GLOB_RECURSE ACTION_CPP_SRCS "action/*.cpp")
 file(GLOB_RECURSE RUNTIME_CPP_SRCS "runtime/*.cpp")
 file(GLOB_RECURSE PROFILER_CPP_SRCS "profiling/*.cpp")
+filter_macos_metadata(SAMPLER_CPP_SRCS)
+filter_macos_metadata(SAMPLER_CU_SRCS)
+filter_macos_metadata(MULTIMODAL_CPP_SRCS)
+filter_macos_metadata(RUNTIME_CPP_SRCS)
+filter_macos_metadata(PROFILER_CPP_SRCS)
 
 # Enable relocatable device code for MoE Marlin kernels
 file(GLOB_RECURSE MOE_MARLIN_CU_SRCS "kernels/moe/moe_marlin/*.cu")
+filter_macos_metadata(MOE_MARLIN_CU_SRCS)
 set_source_files_properties(${MOE_MARLIN_CU_SRCS} PROPERTIES COMPILE_FLAGS
                                                              "-rdc=true")
 
@@ -103,6 +121,7 @@ target_compile_definitions(edgellmKernels PRIVATE ${FMHA_EXCLUDE_DEFINITIONS})
 set_target_properties(edgellmKernels PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
 
 file(GLOB_RECURSE TOKENIZER_CPP_SRCS "tokenizer/*.cpp")
+filter_macos_metadata(TOKENIZER_CPP_SRCS)
 
 add_library(
   edgellmCore STATIC
@@ -119,7 +138,7 @@ add_library(
   ${TOKENIZER_CPP_SRCS})
 target_include_directories(edgellmCore PRIVATE ${COMMON_INCLUDE_DIRS})
 edgellm_xqa_configure_target(edgellmCore)
-target_link_libraries(edgellmCore PRIVATE dl)
+target_link_libraries(edgellmCore PRIVATE dl ${CUBLAS_LIB} ${CUBLASLT_LIB})
 # Apply FMHA SM exclusion definitions
 target_compile_definitions(edgellmCore PRIVATE ${FMHA_EXCLUDE_DEFINITIONS})
 # Enable separable compilation for MoE Marlin kernel templates
@@ -127,6 +146,7 @@ set_target_properties(edgellmCore PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
 
 # Generate builder C++ static library
 file(GLOB_RECURSE BUILDER_CPP_SRCS "builder/*.cpp")
+filter_macos_metadata(BUILDER_CPP_SRCS)
 add_library(edgellmBuilder STATIC ${BUILDER_CPP_SRCS} ${COMMON_CPP_SRCS})
 target_include_directories(edgellmBuilder PRIVATE ${ONNX_PARSER_INCLUDE_DIR}
                                                   ${COMMON_INCLUDE_DIRS})
@@ -134,6 +154,7 @@ target_link_libraries(edgellmBuilder PRIVATE ${NV_ONNX_PARSER_LIB} dl)
 
 # Generate the TensorRT plugin shared libraries
 file(GLOB_RECURSE PLUGIN_CPP_SRCS "plugins/*.cpp")
+filter_macos_metadata(PLUGIN_CPP_SRCS)
 add_library(NvInfer_edgellm_plugin SHARED ${PLUGIN_CPP_SRCS})
 set_target_properties(NvInfer_edgellm_plugin PROPERTIES VERSION 1.0 SOVERSION 1)
 set_target_properties(NvInfer_edgellm_plugin PROPERTIES LIBRARY_OUTPUT_DIRECTORY
@@ -145,7 +166,8 @@ target_compile_definitions(NvInfer_edgellm_plugin
                            PRIVATE ${FMHA_EXCLUDE_DEFINITIONS})
 target_link_libraries(
   NvInfer_edgellm_plugin PRIVATE commonLibraryExt edgellmKernels
-                                 ${CUDA_DRIVER_LIB} ${CUDART_LIB})
+                                 ${CUDA_DRIVER_LIB} ${CUDART_LIB}
+                                 ${CUBLAS_LIB} ${CUBLASLT_LIB})
 add_cross_build_link_options(NvInfer_edgellm_plugin)
 
 # cmake-format: off
